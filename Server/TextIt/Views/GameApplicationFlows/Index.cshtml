@model IEnumerable<TextIt.Models.GameApplicationFlow>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RowSpan)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ColSpan)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StyleString)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IconPath)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RowSpan)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ColSpan)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StyleString)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IconPath)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
